import React from "react";

const Header = () => {
    return (
        <header className="navbar-brand App-header">
            <h1>To Do List</h1>
        </header>
    );
};

export default Header;